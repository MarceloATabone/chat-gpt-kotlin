ktor {
    development = true
    deployment {
        port = 8080
        sslPort = 8443
        port = ${?PORT}
        sslPort = ${?SSL_PORT}
    }
    environment = dev
    environment = ${?KTOR_ENV}
    encryptKey = "ENCRYPT_KEY"
    encryptKey = ${?ENCRYPT_KEY}

    application {
         modules = [ com.mimic.application.ApplicationKt.module ]
    }
    db {
        dbUrl = "jdbc:postgresql://172.30.0.2:5432/postgres"
        dbUrl = ${?DB_URL}
        dbDriver = "org.postgresql.Driver"
        dbUser = "postgres"
        dbUser = ${?DB_USER}
        dbPwd = "ratazana32145"
        dbPwd = ${?DB_PWD}
    }
}
jwt {
    secret = "secret"
    secret = ${?JWT_SECRET}
    issuer = "http://0.0.0.0:8080/"
    issuer = ${?JWT_ISSUER}
    audience = "http://0.0.0.0:8080/"
    audience = ${?JWT_AUDIENCE}
    expiresAt = 172800000 # time to expire Bearer // 172800000 milliseconds = 48 horas
    expiresAt = ${?JWT_EXPIRES_AT}
    realm = "bearer level access authorization"
}